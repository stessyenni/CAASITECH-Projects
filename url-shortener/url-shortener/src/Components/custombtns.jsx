import React from 'react'

const Custombtns = (props) => {
  return (
    <div>
      <button className='w-full border-1 rounded-full'>{props.name}</button>
    </div>
  )
}

export default Custombtns
